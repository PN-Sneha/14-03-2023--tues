#include <stdio.h>

int main(void) {
	int T;
	scanf("%d",T);
	while(T--)
	{
	    long int N,i,sum=0;
	    scanf("%d",&N);
	    long int a[N];
	    for(i=0;i<N;i++)
	    scanf("%d",&a[i]);
	    for(i=0;i<N;i++)
	    sum=sum+a[i];
	    for(i=0;i<N;i++)
	    printf("%d",sum-a[i]);
	}
	return 0;
}
